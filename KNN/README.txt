Comenzamos imputando los valores faltantes con el notebook 1.Preprocesamiento.
Este notebook ha sido implementado por mi compañero Javi.

Después, con el snotebook de transformacion y eliminacion de variables,
eliminamos las que tienen muchos NA's y transformamos las variables en numéricas.
Esto lo llevamos a cabo tomando lo levels de los factores.

Seguidamente hacemos el preprocesamiento de cada etiqueta por separado
en los notebook de python "Preprocesamiento Python...".

Finalmente, con los datos generados en los scripts de python, empleamos el modelo k-NN
en el notebook "implementación del modelo".

En el notebook Ensemble hay un código para leer resultados y hacer la media de los mismos.

